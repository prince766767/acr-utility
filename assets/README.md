Place only the authoritative retained masters here when preparing a final output:

- `UGC_ACR_Form.pdf` - the 30-page master; required for faithful PDF overlay.
- `UGC_ACR_Form.docx` - an official master containing explicit `{{employee.name}}`-style placeholders; required for editable DOCX output.

Do not substitute a recreated template. Keep a checksum of each master and render-compare the generated PDF to it before submitting.
